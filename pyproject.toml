[project]
name = "perf-visualizations"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "altair[all]",
    "numpy",
    "pandas",
    "matplotlib",
    "transformers",
    "torch>=2.6.0",

]
